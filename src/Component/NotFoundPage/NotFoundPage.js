import React from 'react';

const NotFoundPage = () => {
    return (
        <div className=' container d-flex align-items-center justify-content-center vh-100'>
            <h1>Sorry the page did not Found</h1>
        </div>
    );
};

export default NotFoundPage;